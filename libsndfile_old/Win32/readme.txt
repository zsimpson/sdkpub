24 jan 2008 tfb

libsndfile builds with mingw or cygwin.  I downloaded the .dll and gen'd the .lib
and .exp here using VC6 lib.exe.  I also placed the sndfile.h from the win32 binary
distribution here.  (sndfile.h for other platforms is created during ./configure)
